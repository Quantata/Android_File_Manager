package com.example.nyahn_fileexplorer.Models;

public enum  Mode {
    // adpater에서 사용
    BASIC_MODE, SELECTED_MODE,

    // ManageFile에서 사용
    COPY_MODE, MOVE_MODE,

    // Rename
    RENAME_MODE
}
